[project]
name = "geoblacklight.github.io"
requires-python = ">=3.11"
dynamic = ["version"]

[tool.setuptools]
packages = []
py-modules = []

[tool.uv]
package = false

[dependency-groups]
dev = [
    "zensical==0.0.57",
]
